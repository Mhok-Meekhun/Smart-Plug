persistence true
persistence_location /mosquitto/data/
listener 1883 0.0.0.0
allow_anonymous true

# Development-only broker. Production uses a managed MQTT broker with TLS,
# per-device credentials, and topic ACLs.
